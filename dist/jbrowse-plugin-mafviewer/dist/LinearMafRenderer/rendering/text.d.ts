import type { RenderingContext } from './types';
/**
 * Renders text labels for bases when zoom level is sufficient
 */
export declare function renderText(context: RenderingContext, alignment: string, seq: string, leftPx: number, rowTop: number): void;
